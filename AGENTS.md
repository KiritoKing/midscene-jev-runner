# Repository guidance

This repository is an independent, community-maintained Midscene Test runner.
It is not part of the Midscene monorepo and must integrate through public
`@midscene/test` APIs only.

- Use pnpm.
- Keep `@midscene/test` and `playwright` as peer dependencies.
- Do not add private or internal URLs, credentials, production fixtures, or
  non-public benchmark artifacts.
- Preserve the caller-owned `Page`: do not create, navigate, route, or close it.
- Add tests for behavior changes and run `pnpm check` before committing.
- Do not publish, push, or create a pull request without explicit authorization.

## 项目定位与通用性边界

本项目为 Midscene Test 提供通用的 JEV 节点实现，在标准测试编排中与其他节点协作；
`jevAct` 内部允许有界 ReAct 循环。能力应建立在公开浏览器语义、可观察状态和调用方
明确提供的任务目标之上，适用于不同业务、站点和合理的页面实现。

### 1. 禁止业务特化

- 不得根据业务名称、域名、路由、接口、固定字段文案、业务 ID
  或特定操作顺序，添加专属执行分支。
- 不得内置业务流程、业务成功条件、业务数据或某个站点的默认答案。
  业务目标、数据与完成验证由调用方通过公开接口提供。
- 不得以配置项、插件、适配器或改名后的通用术语掩盖业务特例。
  新扩展点必须解决明确的通用能力需求。
- 可以支持通用控件或组件模式，但应优先采用 HTML、ARIA 和实际状态；
  样式名或文案只能作为辅助证据，不能单独决定业务语义。

### 2. 从 badcase 提炼能力

- 允许用具体业务的失败案例定位问题，但修复前必须说明：
  失败机制是什么、对应哪项通用能力、适用范围和不适用范围是什么。
- 修复应作用于观察、候选构造、决策、执行或验证中的通用机制，
  不得复刻该案例的正确操作序列。
- 将业务案例转成最小、公开、无敏感数据的回归样例；
  仅替换业务名称不构成泛化。

### 3. 禁止为测试结果扭曲实现

- 可以先写失败测试再修复，但不得识别测试环境、fixture、
  特定输入或 benchmark，返回预设结果或切换特殊执行路径。
- 不得为使测试通过而弱化断言、绕过完成验证、吞掉失败，
  或将未完成、未知结果改报成功。
- 调整测试预期必须有独立的行为契约依据，
  不能仅因为当前实现无法通过。
- 测试应验证对外行为和通用约束，不能只复述实现中的规则。

### 4. 保留合理的决策空间

- 区分观察事实、启发式推断和任务意图；
  不得把低置信度推断当成确定事实。
- 文案匹配、校验关联和历史经验原则上用于提供上下文或排序。
  硬过滤候选必须有明确、可解释的通用约束依据。
- 不得假设校验错误只能通过直接编辑报错字段解决；
  必须允许合理的前置操作和跨字段依赖。
- 不得用有限关键词规则替代完整任务意图，
  尤其要考虑否定、组合目标、隐含状态变化和不同语言表达。

### 5. 泛化验收

- 每项行为修复除原始回归用例外，必须增加至少一个结构不同、
  但失败机制相同的用例，以及一个不应被该规则限制的反例。
- 按改动范围覆盖相关变化，例如默认值、控件结构、文案、
  前置依赖、异步状态或操作方向；不要求无关组合穷举。
- 单一业务成功、单个 benchmark 提升或测试全绿，
  均不能单独证明泛化能力。
- 交付必须说明通用机制、验证范围、反例和仍未覆盖的边界；
  没有证据的能力不得宣称已支持。

### 6. 执行与完成证据

- 动作调用成功、页面发生变化、模型返回 DONE，
  不等于任务已完成。
- 保持调用方完成验证的独立性；明确区分模型判断完成
  与已通过独立验证完成。
- 执行结果不确定时先观察和读回，不得盲目重放可能产生副作用的动作。
- 循环检测和恢复机制必须基于可观察证据，
  不得仅因某条路径在一个案例中失败就全局禁止该路径。

### 7. 节点协作、页面与 Provider 边界

- `jevAct` 是有步数和时间预算的 ReAct 循环，可在观察、决策与执行之间多轮迭代，
  不能被描述或实现为单一浏览器动作。`DONE` 仍须和独立完成校验区分。
- `jevAct` 不生成文本，也不填充或清空输入；文本由调用方已注册的 Midscene 原生
  `aiInput` 节点负责。`createJevNodes()` 不能假装注册了 `aiInput`，消费者必须通过
  公共 Midscene Agent factory 注册实际可用的节点。
- JEV 节点只能使用调用方提供的 `Page`，不得创建、导航、路由、关闭或接管页面；
  鉴权、前置数据、导航、清理和领域写入门禁仍由调用方节点负责。
- Provider 适配只依赖公开的 System One 请求/响应协议和明确配置，不能按域名、业务
  路由或业务数据分支。候选硬过滤只能依据可观察控件状态或明确安全约束；文本、校验
  错误和业务假设只能辅助上下文或排序。已观察到的重复页面状态或同一失败动作边可用作
  有证据的无进展/循环防护，但不得把关键词匹配或业务经验固化为全局限制。
